// putting this pg under overview, a folder same as cutomer n invoices will only affect the other pgs in the same folder

import DashboardSkeleton from "../../ui/skeletons";


export default function Loading() { //user cn view this pg first while the data loads // loading pg
    return <DashboardSkeleton />;
  }